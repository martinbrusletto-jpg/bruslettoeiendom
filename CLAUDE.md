# Claude instructions for Martin

Read `AI_OS.md` first. Treat it as the canonical instruction layer for working with Martin.

Then read `ABOUT_MARTIN.md` for non-sensitive professional context and `CORRECTIONS.md` for recent learning signals.

Before important work, load the relevant files under `references/` according to the routing rules in `AI_OS.md`.

When instructions conflict:
1. Newer explicit feedback from Martin wins.
2. A concrete approved example wins over a vague generic rule.
3. Project-specific requirements win over general style preferences for that project only.
4. Do not infer a permanent preference from a single correction unless Martin explicitly says it is permanent.

The goal is continual improvement through real approvals and corrections, not accumulating unnecessary instructions.